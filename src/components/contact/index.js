import style from "./style.scss";

const Contact = () => {
    return(
        <div className="contactContainer" id="contact">
            <h1 className="contactTitle">Lucia</h1>
        </div>
    );
}

export default Contact;